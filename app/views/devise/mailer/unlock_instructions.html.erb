<div class="container-fluid main-container">
  <div class="col-md-8 col-sm-10 center-block">
    <h3 class="text-center">Your account has been locked due to an excessive number of unsuccessful sign in attempts.</h3>

    <p><%= link_to 'Unlock my account!', unlock_url(@resource, unlock_token: @token), class: 'btn btn-block btn-lg btn-default btn-primary' %></p>

    <p class="text-center">
      <small>Or you can copy and paste the above url in your browser:</small>
      <br />
      <small><%= link_to unlock_url(@resource, unlock_token: @token), unlock_url(@resource, unlock_token: @token) %><small>
    </p>
  </div>
</div>